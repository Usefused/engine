{
  "name": "fused-mcp-shared-runtime",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Spike: shared MCP runtime exposing exactly search_docs and execute, replacing per-SDK-generated MCP artifacts. See sprint/lighter_mcp_runtime_design.md and sprint/lighter_mcp_runtime_spike_plan.md.",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.7"
  }
}
